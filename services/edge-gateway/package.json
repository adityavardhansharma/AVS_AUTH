{
  "name": "edge-gateway",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@avs-auth/oidc-core": "workspace:*",
    "@avs-auth/types": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250303.0"
  }
}
