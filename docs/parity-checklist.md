# AVS AUTH Shoo Parity Checklist

This checklist is the Phase 1 release gate. AVS AUTH is not ready for beta until every line is implemented, tested, and manually accepted.

## Status Key

- `planned`: specified but not implemented
- `in_progress`: active implementation
- `done`: implemented and covered
- `blocked`: cannot ship until resolved

## Protocol and Broker Parity

| Capability | AVS AUTH Equivalent | Owner | Automated Coverage | Manual Acceptance | Status |
| --- | --- | --- | --- | --- | --- |
| `GET /authorize` | Worker authorize route with PKCE, origin-derived `client_id`, session fast-path | `services/edge-gateway` + `services/convex` | endpoint tests for valid and invalid requests | start sign-in from sample app | planned |
| `POST /token` | Worker token exchange for auth code + PKCE verifier | `services/edge-gateway` + `services/convex` | code redemption, PKCE mismatch, replay rejection | sign-in completes in browser | planned |
| `POST /session/check` | Worker session revalidation endpoint | `services/edge-gateway` + `services/convex` | active, revoked, expired, invalid token cases | revoke consent then observe login required | planned |
| `POST /logout` | Broker logout route revoking session cookie and server session | `services/edge-gateway` + `services/convex` | cookie/session invalidation test | sign out from `/me` | planned |
| OIDC discovery | `/.well-known/openid-configuration` | `packages/oidc-core` + `services/edge-gateway` | metadata contract test | inspect issuer and endpoints in browser | in_progress |
| JWKS publishing | `/.well-known/jwks.json` | `packages/oidc-core` + `services/edge-gateway` | JWKS shape and verification test | verify token with published key | planned |
| Authorization code + PKCE S256 | Required for every browser flow | `packages/oidc-core` + `packages/auth` + `services/edge-gateway` | PKCE generation and verification tests | callback succeeds only with matching verifier | in_progress |
| Browser-friendly token exchange | public client POST to `/token` | `packages/auth` + `services/edge-gateway` | browser flow integration test | vanilla sample app works | planned |
| Domain/origin-derived `client_id` | `origin:${origin}` | `packages/oidc-core` | unit tests for derivation and mismatch rejection | inspect authorize request generated by SDK | in_progress |
| Pairwise subject ids | HMAC-based per-user, per-origin subject | `packages/oidc-core` + `services/convex` | stable and cross-origin uniqueness tests | same app stable, different app different | planned |
| `id_token` issuance | ES256 JWT with OIDC claims | `packages/oidc-core` + `services/edge-gateway` | signing and verification test | decode sample token from real flow | planned |
| Optional PII request flow | `pii=true` consent and claim gating | `services/edge-gateway` + `services/convex` + broker UI | consent behavior tests | allow or deny profile data | planned |
| Broker session reuse | central cookie on `auth.adityavs.tech` | `services/edge-gateway` + `services/convex` | repeat sign-in path test | second app signs in without Google | planned |

## Identity and Token Model

| Capability | AVS AUTH Equivalent | Owner | Automated Coverage | Manual Acceptance | Status |
| --- | --- | --- | --- | --- | --- |
| `sub === pairwise_sub` | claim generation contract | `packages/oidc-core` | token claim test | inspect decoded token | planned |
| `iss`, `aud`, `sub`, `iat`, `exp`, `jti` | required claims | `packages/oidc-core` | token claim test | verify in app and server docs | planned |
| optional `email`, `email_verified`, `name`, `picture` | consent-gated optional claims | `packages/oidc-core` + `services/convex` | PII claim gating test | consent grant toggles claims | planned |
| `nonce` passthrough | optional security addition | `services/edge-gateway` + `packages/oidc-core` | nonce persistence test | nonce present in token when requested | planned |
| ES256 signing | signing key set and `kid` rotation | `packages/oidc-core` + `services/convex` | JWT verification against JWKS | rotate active key and verify | planned |

## Broker UX Parity

| Page or Flow | AVS AUTH Equivalent | Owner | Automated Coverage | Manual Acceptance | Status |
| --- | --- | --- | --- | --- | --- |
| `/` landing page | broker intro, docs, package install, legal links | `apps/broker-web` | route rendering smoke test | open home signed out and signed in | planned |
| `/sign-in` | Google sign-in and fast-path when session exists | `apps/broker-web` + `services/edge-gateway` | auth ticket handling test | existing session skips friction | planned |
| `/consent` | origin, PII request, grant or deny | `apps/broker-web` + `services/convex` | consent form tests | grant and deny paths both work | planned |
| `/me` | broker profile, picture, email, sites summary, sign out | `apps/broker-web` + `services/convex` | authenticated route test | see profile details and sign out | planned |
| `/authorized-sites` | consent list with revoke action | `apps/broker-web` + `services/convex` | revoke flow test | revoked origin disappears or updates | planned |
| `/privacy` | privacy notice | `apps/broker-web` | route smoke test | page linked in footer | planned |
| `/terms` | terms page | `apps/broker-web` | route smoke test | page linked in footer | planned |
| `/error` | safe user-facing error with correlation id | `apps/broker-web` + `services/edge-gateway` | error rendering test | correlation id visible | planned |
| `/no-session` | prompt to sign in and return home | `apps/broker-web` | route smoke test | redirect users here when needed | planned |

## SDK and Package Parity

| Capability | AVS AUTH Equivalent | Owner | Automated Coverage | Manual Acceptance | Status |
| --- | --- | --- | --- | --- | --- |
| Hosted browser script | `/avs-auth.js` with `window.AvsAuth` | `services/edge-gateway` + `packages/auth` | browser global smoke test | script tag integration works in plain HTML | planned |
| Hosted script auto-upgrades authorize links | link bootstrap behavior | `services/edge-gateway` | DOM integration test | click annotated authorize link | planned |
| Hosted script auto-handles callback | callback path handling | `services/edge-gateway` + `packages/auth` | callback integration test | refresh callback route safely | planned |
| Local identity persistence | `localStorage` helpers | `packages/auth` | persistence tests | identity survives reload | in_progress |
| Read and clear identity helpers | `getIdentity`, `clearIdentity` | `packages/auth` | helper tests | manual sign out clears cache | in_progress |
| Create/start/finish sign-in helpers | SDK parity helpers | `packages/auth` | function surface tests | successful full browser flow | in_progress |
| Session monitor and session check helpers | background revocation awareness | `packages/auth` | interval and revoke tests | app receives login required | in_progress |
| JWT decode helper | `decodeIdentityClaims` | `packages/auth` | decode test | claims visible in client app | in_progress |
| `@avs-auth/types` | shared protocol and SDK types | `packages/types` | export contract test | package docs reference types correctly | in_progress |
| `@avs-auth/auth` | browser SDK package | `packages/auth` | API surface test | vanilla example works | in_progress |
| `@avs-auth/react` | React wrapper package | `packages/react` | hook behavior test | React example works | in_progress |

## Docs and DX Parity

| Capability | AVS AUTH Equivalent | Owner | Automated Coverage | Manual Acceptance | Status |
| --- | --- | --- | --- | --- | --- |
| Intro docs | docs introduction | `apps/docs` | docs build | page explains product and parity | planned |
| Getting Started (React) | install and hook flow | `apps/docs` | docs build | React quickstart works | planned |
| Getting Started (Vanilla JS) | hosted script and package flow | `apps/docs` | docs build | vanilla quickstart works | planned |
| How It Works | protocol walkthrough | `apps/docs` | docs build | architecture page matches behavior | planned |
| Server Verification | JWT verification and JWKS docs | `apps/docs` | code sample lint if added | relying party server verifies token | planned |
| Convex integration guide | Convex wiring guide | `apps/docs` + `services/convex` | docs build | guide covers deployment model | planned |
| API reference | all public SDK functions and options | `apps/docs` + packages | docs build | package README and docs agree | planned |
| Hosted script “auth in 2 lines” | script quickstart | `apps/docs` + `services/edge-gateway` | docs build | copy-paste sample works | planned |
| Zero dashboard registration | origin-derived client model | `packages/oidc-core` + docs | docs build | local and prod examples both work | planned |
