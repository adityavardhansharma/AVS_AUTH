# AVS AUTH Shoo Parity Checklist

This checklist is the Phase 1 release gate. AVS AUTH is not ready for beta until every line is implemented, tested, and manually accepted.

## Status Key

- `planned`: specified but not implemented
- `in_progress`: active implementation
- `done`: implemented and covered

## Protocol and Broker Parity

| Capability | AVS AUTH Equivalent | Owner | Automated Coverage | Manual Acceptance | Status |
| --- | --- | --- | --- | --- | --- |
| `GET /authorize` | Worker authorize route with PKCE, origin-derived `client_id`, session fast-path | `services/edge-gateway` + `services/convex` | endpoint tests for valid and invalid requests, fast-path test | start sign-in from sample app | done |
| `POST /token` | Worker token exchange for auth code + PKCE verifier | `services/edge-gateway` + `services/convex` | code redemption, PKCE mismatch, replay rejection, redirect mismatch, CORS | sign-in completes in browser | done |
| `POST /session/check` | Worker session revalidation endpoint | `services/edge-gateway` + `services/convex` | active, revoked, expired, invalid token, expired token cases | revoke consent then observe login required | done |
| `POST /logout` | Broker logout route revoking session cookie and server session | `services/edge-gateway` + `services/convex` | cookie/session invalidation test | sign out from `/me` | done |
| OIDC discovery | `/.well-known/openid-configuration` | `packages/oidc-core` + `services/edge-gateway` | metadata contract test | inspect issuer and endpoints in browser | done |
| JWKS publishing | `/.well-known/jwks.json` | `packages/oidc-core` + `services/edge-gateway` | JWKS shape test, multi-key serving | verify token with published key | done |
| Authorization code + PKCE S256 | Required for every browser flow | `packages/oidc-core` + `packages/auth` + `services/edge-gateway` | PKCE generation, verification, mismatch rejection | callback succeeds only with matching verifier | done |
| Browser-friendly token exchange | public client POST to `/token` | `packages/auth` + `services/edge-gateway` | full PKCE flow integration test | vanilla sample app works | done |
| Domain/origin-derived `client_id` | `origin:${origin}` | `packages/oidc-core` | unit tests for derivation and mismatch rejection | inspect authorize request generated by SDK | done |
| Pairwise subject ids | HMAC-based per-user, per-origin subject | `packages/oidc-core` + `services/convex` | stable and cross-origin uniqueness tests | same app stable, different app different | done |
| `id_token` issuance | ES256 JWT with OIDC claims | `packages/oidc-core` + `services/edge-gateway` | signing and verification test, full flow produces valid token | decode sample token from real flow | done |
| Optional PII request flow | `pii=true` consent and claim gating | `services/edge-gateway` + `services/convex` + broker UI | consent behavior tests | allow or deny profile data | done |
| Broker session reuse | central cookie on `auth.adityavs.tech` | `services/edge-gateway` + `services/convex` | repeat sign-in path test (fast-path) | second app signs in without Google | done |

## Identity and Token Model

| Capability | AVS AUTH Equivalent | Owner | Automated Coverage | Manual Acceptance | Status |
| --- | --- | --- | --- | --- | --- |
| `sub === pairwise_sub` | claim generation contract | `packages/oidc-core` | token claim test | inspect decoded token | done |
| `iss`, `aud`, `sub`, `iat`, `exp`, `jti` | required claims | `packages/oidc-core` | token claim test | verify in app and server docs | done |
| optional `email`, `email_verified`, `name`, `picture` | consent-gated optional claims | `packages/oidc-core` + `services/convex` | PII claim gating test | consent grant toggles claims | done |
| `nonce` passthrough | optional security addition | `services/edge-gateway` + `packages/oidc-core` | nonce persistence test | nonce present in token when requested | done |
| ES256 signing | signing key set and `kid` rotation | `packages/oidc-core` + `services/convex` | JWT verification against JWKS, key generation test | rotate active key and verify | done |

## Broker UX Parity

| Page or Flow | AVS AUTH Equivalent | Owner | Automated Coverage | Manual Acceptance | Status |
| --- | --- | --- | --- | --- | --- |
| `/` landing page | broker intro, docs, package install, legal links | Worker inline HTML | route rendering smoke test | open home signed out and signed in | done |
| `/sign-in` | Google sign-in and fast-path when session exists | Worker inline HTML + `services/edge-gateway` | auth ticket handling test | existing session skips friction | done |
| `/consent` | origin, PII request, grant or deny | Worker inline HTML + `services/convex` | consent form tests | grant and deny paths both work | done |
| `/me` | broker profile, picture, email, sites summary, sign out | Worker inline HTML + `services/convex` | authenticated route test | see profile details and sign out | done |
| `/authorized-sites` | consent list with revoke action | Worker inline HTML + `services/convex` | revoke flow test | revoked origin disappears or updates | done |
| `/privacy` | privacy notice | Worker inline HTML | route smoke test | page linked in footer | done |
| `/terms` | terms page | Worker inline HTML | route smoke test | page linked in footer | done |
| `/error` | safe user-facing error with correlation id | Worker inline HTML + `services/edge-gateway` | error rendering test | correlation id visible | done |
| `/no-session` | prompt to sign in and return home | Worker inline HTML | route smoke test | redirect users here when needed | done |

## SDK and Package Parity

| Capability | AVS AUTH Equivalent | Owner | Automated Coverage | Manual Acceptance | Status |
| --- | --- | --- | --- | --- | --- |
| Hosted browser script | `/avs-auth.js` with `window.AvsAuth` | `services/edge-gateway` + `packages/auth` | browser global smoke test, callback handling, login-required dispatch, strict session check, aud validation | script tag integration works in plain HTML | done |
| Hosted script auto-upgrades authorize links | link bootstrap behavior | `packages/auth` | DOM integration test | click annotated authorize link | done |
| Hosted script auto-handles callback | callback path handling | `packages/auth` | callback integration test | refresh callback route safely | done |
| Local identity persistence | `localStorage` helpers | `packages/auth` | persistence tests | identity survives reload | done |
| Read and clear identity helpers | `getIdentity`, `clearIdentity` | `packages/auth` | helper tests | manual sign out clears cache | done |
| Create/start/finish sign-in helpers | SDK parity helpers | `packages/auth` | function surface tests | successful full browser flow | done |
| Session monitor and session check helpers | auto-stop on `login_required`, strict 200/401 parsing, aud pre-validation, throw on unexpected status | `packages/auth` | auto-stop test, aud mismatch test, malformed 401 test, unexpected status throw test | app receives login required | done |
| JWT decode helper | `decodeIdentityClaims` | `packages/auth` | decode test | claims visible in client app | done |
| `@avs-auth/types` | shared protocol and SDK types | `packages/types` | export contract test (no `--passWithNoTests`) | package docs reference types correctly | done |
| `@avs-auth/auth` | browser SDK package | `packages/auth` | API surface test, checkSession Shoo parity tests | vanilla example works | done |
| `@avs-auth/react` | React wrapper package | `packages/react` | hook behavior test, token-based session state, monitor-driven transitions | React example works | done |

## Docs and DX Parity

| Capability | AVS AUTH Equivalent | Owner | Automated Coverage | Manual Acceptance | Status |
| --- | --- | --- | --- | --- | --- |
| Intro docs | docs introduction | `apps/docs` | docs build | page explains product and parity | done |
| Getting Started (React) | install and hook flow | `apps/docs` | docs build | React quickstart works | done |
| Getting Started (Vanilla JS) | hosted script and package flow | `apps/docs` | docs build | vanilla quickstart works | done |
| How It Works | protocol walkthrough | `apps/docs` | docs build | architecture page matches behavior | done |
| Server Verification | JWT verification and JWKS docs | `apps/docs` | code sample lint if added | relying party server verifies token | done |
| Convex integration guide | Convex wiring guide | `apps/docs` + `services/convex` | docs build | guide covers deployment model | done |
| API reference | all public SDK functions and options | `apps/docs` + packages | docs build | package README and docs agree | done |
| Hosted script "auth in 2 lines" | script quickstart | `apps/docs` + `services/edge-gateway` | docs build | copy-paste sample works | done |
| Zero dashboard registration | origin-derived client model | `packages/oidc-core` + docs | docs build | local and prod examples both work | done |

## Hardening (Pre-Beta)

| Capability | AVS AUTH Equivalent | Owner | Automated Coverage | Status |
| --- | --- | --- | --- | --- |
| Rate limiting | `/authorize` and `/token` per-origin limits using `origin_metrics` | `services/convex` + `services/edge-gateway` | 429 on limit exceeded (Convex mode) | in_progress |
| Key rotation workflow | `/admin/rotate-keys` + multi-key JWKS serving | `services/edge-gateway` + `services/convex` | admin route tests, rotation test | in_progress |
| Operator tooling | `/admin/*` protected routes for block/revoke/audit | `services/edge-gateway` + `services/convex` | auth-required tests, block/unblock/revoke/audit tests | in_progress |
| Convex function tests | full test suite for all function modules | `services/convex` | all function tests | planned |
| Token exchange E2E tests | `/token` success, replay, PKCE mismatch, redirect mismatch | `services/edge-gateway` | protocol tests | done |
| Remove `passWithNoTests` | all packages enforce test presence | all packages | test runner | done |
